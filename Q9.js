function calcularRaizQuadrada(numero) {
  let resultado = numero ** (1 / 2);
  return resultado;
}
console.log(calcularRaizQuadrada(16));
/**** Escreva o código acima ☝️******/

//Não altere as linhas abaixo
module.exports = {
  calcularRaizQuadrada,
};
